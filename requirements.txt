# Runtime dependencies, pinned for reproducible installs.
# (Unpinned ranges previously let pandas 3.0 land and break Styler.applymap —
# pin exact versions and bump deliberately.)
streamlit==1.58.0
streamlit-autorefresh==1.0.1
yfinance==1.5.1
pandas==3.0.3
numpy==2.4.6
plotly==6.8.0
anthropic==0.116.0
python-dotenv==1.2.2
lxml==6.1.1
pdfplumber==0.11.10
# Only needed if you point DATABASE_URL at Postgres; SQLite is the default.
psycopg[binary]==3.3.4
